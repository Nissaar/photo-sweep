<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
	<id>photosweep</id>
	<name>Photo Sweep</name>
	<summary>Go through your photo library one photo at a time, month by month, keeping or deleting each one.</summary>
	<description><![CDATA[
Photo Sweep turns the job of tidying a large photo library into something you can
actually finish: pick a month, judge one photo at a time, then review everything you
marked before a single file is touched.

- **Month by month.** Your photos and videos are indexed by the date they were taken,
  not the date they were uploaded. Months you have finished move out of the way.
- **One at a time.** Keep or delete each photo with a swipe, a click or the keyboard.
  Undo steps back.
- **Nothing happens until you confirm.** Verdicts are held as a pending list. The
  review screen is the only place that changes your files, and it shows you everything
  first.
- **Two ways to delete.** Move files to the Nextcloud trash, where your server's
  retention policy keeps them recoverable, or collect them in a folder and delete them
  yourself in Files.

The index is built from your own files. The Photos app's extracted EXIF capture date
is used when it is available, and Photo Sweep falls back to the filename and the
modification time when it is not, so it works on any install.

A companion Android app talks to this app over its REST API, so you can do the same
review from your phone.
	]]></description>
	<version>1.0.1</version>
	<licence>agpl</licence>
	<author mail="mushir.gopee@aqr8.fr">Nissaar</author>
	<namespace>PhotoSweep</namespace>
	<category>multimedia</category>
	<category>files</category>
	<website>https://github.com/Nissaar/photo-sweep</website>
	<bugs>https://github.com/Nissaar/photo-sweep/issues</bugs>
	<repository type="git">https://github.com/Nissaar/photo-sweep.git</repository>
	<!--
		Served from the repository rather than uploaded: the store renders whatever is
		at this URL, so a screenshot stays in step with the app by being committed
		next to it. The schema requires https and this position, between <repository>
		and <dependencies>.
	-->
	<screenshot>https://raw.githubusercontent.com/Nissaar/photo-sweep/main/screenshots/review.png</screenshot>
	<dependencies>
		<php min-version="8.1"/>
		<nextcloud min-version="31" max-version="34"/>
	</dependencies>
	<background-jobs>
		<job>OCA\PhotoSweep\BackgroundJob\IndexJob</job>
	</background-jobs>
	<commands>
		<command>OCA\PhotoSweep\Command\IndexCommand</command>
	</commands>
	<navigations>
		<navigation>
			<id>photosweep</id>
			<name>Photo Sweep</name>
			<route>photosweep.page.index</route>
			<icon>app.svg</icon>
			<order>15</order>
		</navigation>
	</navigations>
</info>
